# Local Agent Rule

Use Antigravity for testing and reporting against this repository. Do not redesign business rules, add schema fields, or replace working architecture. Only modify code when explicitly authorized for a specific defect.
